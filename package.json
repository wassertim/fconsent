{
  "scripts": {
    "test": "jest",
    "dist": "mkdir -p dist && git archive --format zip --output ./dist/$(git describe --tags --always).zip HEAD"
  },
  "devDependencies": {
    "@types/expect-puppeteer": "^5.0.1",
    "@types/jest": "^29.1.1",
    "@types/jest-environment-puppeteer": "^5.0.2",
    "@types/puppeteer": "^5.4.6",
    "jest": "^29.1.2",
    "jest-puppeteer": "^6.1.1",
    "puppeteer": "^18.0.5",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
